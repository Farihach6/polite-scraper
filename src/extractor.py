"""
extractor.py — HTML parsing -> raw records.

Not yet implemented. Lands in Stage 3 (extract the raw records):
parse each cached book page with BeautifulSoup and pull the 8 raw
fields (title, product_url, price_text, availability_text,
rating_text, description, source_page, fetched_at).
"""
